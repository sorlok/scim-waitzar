icondir = $(SCIM_ICONDIR)
icon_DATA = scim-waitzar.png
files_MODEL = Myanmar.model
files_MYWORDS = mywords.txt
files_CONFIG = config.txt

pkgdata_DATA = \
	Myanmar.model \
	mywords.txt \
	config.txt

EXTRA_DIST = $(icon_DATA) $(files_MYWORDS) $(files_MODEL) $(files_CONFIG)

